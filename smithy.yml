project: golang
language: golang

script:
  - echo "All Ur Moniez Are Belong To Gnatsd"
  - go get -d ./...
  - cd ./cheapskate && go build -x .
  - cd ./client && go build -x .

artifacts:
  release:
    - cheapskate/cheapskate
    - client/client
